export interface PostCardItemSchema {
  id: number;
  imageUrl: string;
  title: string;
  thumbnailUrl: string;
  point: number;
  target: string;
  questionCount: number;
}

export interface CarouselItemSchema {
  id: number;
  imageUrl: string;
  title: string;
}
